name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.14"
          cache: pip
      - run: |
          set -euxo pipefail
          python -m pip install --upgrade pip==26.2
          python -m pip install \
            -e '.[dev]' \
            -r operations/requirements.txt \
            -r host-operations/requirements.txt
          python -m pip check
      - run: pytest -q
      - run: python -m build
      - name: Package first-party operations release
        run: |
          set -euo pipefail
          operations_version="$(tr -d '\n' < operations/VERSION)"
          git archive \
            --format=tar.gz \
            --output="dist/atlas-operations-${operations_version}.tar.gz" \
            HEAD:operations
      - name: Package first-party host operations release
        run: |
          set -euo pipefail
          host_operations_version="$(tr -d '\n' < host-operations/VERSION)"
          git archive \
            --format=tar.gz \
            --output="dist/atlas-host-operations-${host_operations_version}.tar.gz" \
            HEAD:host-operations
      - run: sha256sum dist/* > release-checksums.txt
      - uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          fail_on_unmatched_files: true
          files: |
            dist/*
            release-checksums.txt
