| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 候補語 | 初出定義 |
| --- | --- | --- | --- | --- | --- | --- |
| `CODEX-INSTRUCTIONS.md` 1, 3, 13 / `DESIGN.md` 1–3, 18 | Atlas core を変えず、first-party operation の利用者向け入口を限定する | PATH に公開する operation command を `configctl`, `hostctl`, `imagectl`, `providerctl`, `operationctl` の 5 個にする | 目的 | 既存 primitive の contract を維持した後、公開 command を controller へ置き換える | public command surface | —（設計文書の既存語） |
| `CODEX-INSTRUCTIONS.md` 4 PR 2, 8 / `DESIGN.md` 9, 15 | Ansible の既存 argv と実行結果を変えずに configuration project の操作を一つの入口へまとめる | `config-*` と `inventory-show` の処理を `configuration-operations` release の `configctl` subcommand から呼ぶ | 手段 | controller を追加し、caller を移行してから旧 public command を削除する | `configuration-operations`, `configctl` | —（設計文書の既存語） |
| `CODEX-INSTRUCTIONS.md` 4 PR 3–5, 8 / `DESIGN.md` 10–15 | provider と resource lifecycle の公開入口を domain ごとに分け、内部 phase を PATH から隠す | 現行 `hostctl` の lifecycle、Proxmox image lifecycle、provider 診断、artifact 診断を `infrastructure-operations` release に置き、phase 実行を job にする | 手段 | 既存 safety contract を保ったまま controller を追加し、最終 manifest では job だけが primitive implementation を参照する | `infrastructure-operations`, `hostctl`, `imagectl`, `providerctl`, `operationctl` | —（設計文書の既存語） |
| `CODEX-INSTRUCTIONS.md` 5–7 / `DESIGN.md` 7, 16 | controller と child process の UNIX contract を固定する | argv list、`shell=False`、継承した cwd/environment/stdout/stderr、child exit code、missing executable の 127、signal exit を保持する | 手段 | controller parser が入力を確定した後、module の直接 import ではなく executable または `atlas job run` を起動する | child process contract | —（設計文書の既存語） |
| `CODEX-INSTRUCTIONS.md` 3, 9 / `DESIGN.md` 7–13 | mutation の安全性と診断結果を公開名の変更から分離して維持する | plan/evidence validation、source digest、confirmation、ownership marker、rollback safety、stdout/stderr、exit code 0–6 を既存 test と追加 test で固定する | 記録 | 現行 command の contract を一覧化し、その後 controller と job の同値性を検証する | safety contract | —（設計文書の既存語） |
| `CODEX-INSTRUCTIONS.md` 4 PR 6–9, 10–13 / `DESIGN.md` 17–18 | 旧 command と旧 `operations` release を残さず最終状態へ移行する | repository 内 caller、CI、release archive、Docker smoke test、operator docs を新 release と 5 controller に更新し、旧 public shim と旧 release を削除する | 目的 | caller 移行と test 更新の後に旧 command を manifest から外し、全検証の成功後に PR を ready にする | caller migration, old release retirement | —（設計文書の既存語） |
